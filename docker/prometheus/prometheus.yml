    # my global config
global:
  scrape_interval:     15s
  evaluation_interval: 15s
  # scrape_timeout is set to the global default (10s).

rule_files:
#- "first.rules"
#- "my/*.rules"



scrape_configs:
- job_name: monitoring


  static_configs:
  - targets: ['host.docker.internal']
